{
    "name": "catan-helper",
    "version": "1.0.0",
    "scripts": {
      "postinstall": "cd client && npm install && npm run build && mv build ../static"
    }
    ,
    "engines": {
      "node": "18.x",  
      "npm": "9.x"    
    }
    
  }